# NOTE: opencv-contrib-python (NOT opencv-python) — the LBPH recognizer lives in
# the cv2.face module, which ships only in the contrib build.
opencv-contrib-python>=4.8
numpy>=1.24
streamlit>=1.30

# Doc-only: needed solely to regenerate docs/PROJECT_DOCUMENTATION.docx via
# docs/generate_project_doc.py. Not required to run the app.
python-docx>=1.1
